<!--{#
  Renders a section with title, subtitle and items.
  Arguments:
    @param {string} title (optional): Section title.
    @param {string} subtitle (optional): Text for a subtitle beneath the primary title.
    @param {array} items: List of item objects.
    @param {object} modifiers (optional): Object with modifier styles for the grid.
    @param {string} modifiers.variant: if 'stories' it will render stories-card.
 #}-->
{% from 'item-grid.njk' import itemGrid %} {% macro storiesSection(items, modifiers) %} {% if items.length > 0 %}
<section class="stories-section">
  {% if title %}
  <h3 class="stories-section__title type--h2">{{title}}</h3>
  {% endif %} {% if subtitle %}
  <p class="type--base stories-section__subtitle">{{subtitle}}</p>
  {% endif %}

  <div class="stories-section__grid">{{ itemGrid('', items, modifiers) }}</div>
</section>
{% endif %} {% endmacro %}
